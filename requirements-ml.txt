# Optional: local DistilBERT intent classifier + training/eval scripts.
# Install only when you use `ai_models/` (see main.py) or run training/*.py
-r requirements.txt
torch>=2.0.0
transformers>=4.30.0
scikit-learn>=1.3.0
pandas>=2.0.0
